
server:
  port: 5454
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/instagram_v2
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

